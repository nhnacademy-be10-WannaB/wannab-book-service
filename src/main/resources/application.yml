spring:
  application:
    name: book-service
  config:
    import: optional:classpath:properties/env.properties

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
      database: ${REDIS_DATABASE}