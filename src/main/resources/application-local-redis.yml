server:
  port: 8081

spring:
  application:
    name: book-service
  import: optional:classpath:properties/env.properties
  profiles:
    active: h2
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
      database: ${REDIS_DATABASE}
