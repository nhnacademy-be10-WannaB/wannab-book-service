spring:
  application:
    name: book-service
  lifecycle:
    timeout-per-shutdown-phase: 30s
  cloud:
    discovery:
      enabled: false
    config:
      enabled: false

server:
  shutdown: graceful

eureka:
  client:
    enabled: false

aladin:
  api:
    base-url: "http://www.aladin.co.kr/ttb/api/"
    ttbkey: "dummy-ttb-key-for-ci"