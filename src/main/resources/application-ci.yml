spring:
  application:
    name: book-service
  lifecycle:
    timeout-per-shutdown-phase: 30s
  h2:
    console:
      enabled: true
      path: /h2-console
  data:
    redis:
      host: 11.11.11.11
      port: 6666
      password: secret
      database: 333
  cloud:
    discovery:
      enabled: false
    config:
      enabled: false

dbcp2:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
    initial-size: 5
    max-total: 5
    max-idle: 5
    min-idle: 5
    between-eviction-millis: 300000
    min-evictable-idle-millis: 600000
    num-tests-per-eviction-run: 3

server:
  port: 10390
  shutdown: graceful

eureka:
  client:
    enabled: false

management:
  health:
    elasticsearch:
      enabled: false

aladin:
  api:
    base-url: "http://secret/"
    ttb-key: "secret"

elasticsearch:
  host: localhost
  port: 9200
  username: secret
  password: secret

search:
  index: wannab_books_v3
  pipeline: wannab-chosung-pipeline
  boost:
    fields:
      title: 5.0
      title_chosung_full : 2.0
      title_chosung_word : 3.0
      authors: 3.0
      tags: 2.0
      description: 1.0
      publishers: 1.0
  chosung-title: title_chosung_full
  chosung-word: title_chosung_word
